/** Practice solve pages grow with content; scroll happens on `main` in app shell. */
export default function PracticeQuestionLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
